# 安全策略

## 支持范围

项目仍在快速迭代，目前只维护 `main` 分支的最新版本。旧提交和第三方平台已经失效的页面流程不单独提供安全修复。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security > Report a vulnerability** 私下报告。不要在公开 Issue、讨论、截图或日志中提交 API Key、Token、Cookie、浏览器资料或真实账号信息。

报告中请包含：

- 受影响的提交或版本
- 可复现步骤和预期影响
- 已确认的攻击前提
- 脱敏后的日志或最小复现

维护者会尽快确认报告。修复公开前，请给维护者合理的调查和发布窗口。

## 部署边界

墨舟是本地单用户工具，目前没有系统用户认证、租户隔离或本地密钥加密。默认监听 `127.0.0.1`，请勿直接暴露到公网。远程部署必须自行增加身份认证、HTTPS、网络访问控制和密钥管理。

平台登录状态保存在 `data/browser_profiles/`，应用配置保存在 `data/publisher.db`。请将整个 `data/` 视为敏感数据，限制访问权限并谨慎备份。发现凭据误提交后，应立即在对应平台撤销或轮换凭据；仅从 Git 历史删除文件不能使已泄露的凭据重新安全。
